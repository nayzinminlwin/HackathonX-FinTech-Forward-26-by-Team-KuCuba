name: kucuba_backend
description: KuCuba Scam Detector Backend - Dart Shelf server for POST /analyze
version: 1.0.0

environment:
  sdk: ^3.6.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  http: ^1.3.0
  dotenv: ^4.2.0
  google_generative_ai: ^0.4.6
