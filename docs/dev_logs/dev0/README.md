# `dev0` development logs

Logs for git branch **`dev0`** — **backend** work (`backend/`, `POST /analyze`, Safe Browsing, Gemini).

Use this folder only (not `dev1/`). See [`../README.md`](../README.md).
